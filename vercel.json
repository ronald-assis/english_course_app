{
  "buildCommand": "pnpm build",
  "installCommand": "pnpm i --frozen-lockfile --registry=https://registry.npmjs.org",
  "framework": "nextjs",
  "env": { "NODE_VERSION": "20" },
  "github": { "enabled": true }
}